import type { ExtensionAPI } from "@earendil-works/pi-coding-agent"

// Skills are available via the vendor path registered in VENDOR_SKILL_PATHS.
// No system prompt injection needed — users discover them naturally.
export default function superpowersExtension(_pi: ExtensionAPI) {}
